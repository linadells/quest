import 'package:flutter/material.dart';

class AllQuests extends StatelessWidget {
  const AllQuests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}